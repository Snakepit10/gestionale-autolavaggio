# Utilizziamo il modello Postazione definito in apps.core
# per evitare duplicazioni e conflitti

from apps.core.models import Postazione

# Il modello Postazione è definito in apps.core.models
# Questo file serve solo per mantenere la struttura dell'app